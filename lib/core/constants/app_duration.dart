class AppDurations {
  static const Duration short = Duration(milliseconds: 200);
  static const Duration medium = Duration(milliseconds: 400);
  static const Duration long = Duration(milliseconds: 800);
  static const Duration yarimSecond = Duration(milliseconds: 500);
  static const Duration birSecond = Duration(seconds: 1);
  static const Duration ikiSecond = Duration(seconds: 2);
}